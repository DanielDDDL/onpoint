<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    <entry key="SELECT_SINCE">
        SELECT id, when_happened, marked_date, marked_type_id
        FROM mark
        WHERE when_happened > ?
    </entry>
</properties>